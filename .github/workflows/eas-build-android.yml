# DISABLED — replaced by android-native-build.yml (Gradle, no EAS cloud)
# This file is kept as a placeholder so git history is clean.
name: EAS Build Android (disabled)
on:
  workflow_dispatch:
    inputs:
      _disabled:
        description: "This workflow is disabled. Use 'Build Android APK' instead."
        required: true
        default: "disabled"
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow has been replaced by android-native-build.yml" && exit 1
